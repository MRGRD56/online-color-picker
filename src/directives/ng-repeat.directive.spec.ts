import { NgRepeatDirective } from "./ng-repeat.directive";

describe("NgRepeatDirective", () => {
    it("should create an instance", () => {
        const directive = new NgRepeatDirective();
        expect(directive).toBeTruthy();
    });
});
